<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="company.com.to_do_list">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_latest"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_latest_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.activities.WorkTypeActivity"
            android:label="" />
        <activity
            android:name=".views.activities.BottomBarActivity"
            android:label="@string/title_activity_bottom_bar" />
        <receiver android:name=".AlarmNotificationReceiver"/>
        <activity
            android:name=".views.activities.SplashScreenActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>